# 版本说明

面向二次开发者的版本变更记录：只写对使用者的影响（新能力、行为变化、是否需要改代码/配置），不写内部实现细节。倒序排列；`Unreleased` 段随功能/修复 MR 实时追加，发版时归档为正式版本节并移除（该段仅在开发期存在，归档后由下一个功能/修复 MR 重新建立）。

## 1.1.2 - 2026-08-31

- 新增：本地视频录制与回放——相机页收敛为「拍照 / 录像」分段切换与中央主快门（录像时标题栏显示红点与秒级时长），共享 MJPEG 摄像头流录制为本地 AVI（麦克风可用时附 PCM 音轨，单段限 110 秒，保存到 SD 卡），应用中心新增视频列表（卡片列表 + 沉浸式播放器，支持选择/全选/异步批量删除）；需启用 `ENABLE_TY_AVI_MEDIA` 与 `UI_FEATURE_VIDEO`。
- 优化：录像与回放稳定性——录像限 15fps 并按实际采集时间戳封装，存储慢写/异常时自动降帧、安全停止并提示存储异常；回放以首帧+音频就绪启动媒体时钟、暂停恢复重建音画时间轴，截断/损坏的 AVI 明确提示播放错误；视频、AI 播放与 P2P 通话经共享扬声器仲裁，避免混音与误关功放。
- 修复：P2P 通话挂断后相机可能整体黑屏失效——H264 切回 JPEG 失败时自动重试与回滚，仍失败也会在下一次模式切换时自愈重建，不再需要重启恢复。
- 调整：板型支持变化——移除 XY4100LC 板型；L511_Y7PM 完善发版配置并新增 LCD TEF 动画刷屏。

## 1.1.0 - 2026-08-14

- 新增：全新 UI 框架与整套界面——从零构建 Vue-inspired LVGL 响应式框架（状态存储/页面路由/定时驱动），落地主屏、AI 对话、应用中心、设置、相册、音乐、录音、时钟、联系人等全部页面，中英双语可运行时切换（框架说明见 `src/ui/README.md`，开发约束见 `src/ui/RULES.md`）。
- 新增：UI 按功能簇裁剪与板级定制——`UI_FEATURE_*` Kconfig 开关按功能簇裁剪页面/服务（关闭项经降级桩优雅链接），板级可注册自有界面替代标准 wukong 页面。
- 新增：TEF 动图管线全面替代 GIF——自研 TEF 格式与编码工具链（含 `--stabilize` 时域降噪），覆盖表情直推 GRAM、开机动画、页面 `lv_tef` 控件三种播放形态，省约 1.1MB flash、解码 CPU 降至约 1/6（用法见[TEF 动图](docs/howto/use-tef-animation.md)）。
- 新增：EYES 双屏板型支持——两块 st7735s 拼接为单逻辑屏，TEF 表情双 panel 并行直推，配合驱动刷新方向修复显著减轻双屏动画割裂感。
- 优化：音频播放稳定性与诊断——TTS 流式蓄水消除网络抖动碎卡、修复相机预览时音乐持续卡顿（播放线程优先级）、AP-STAT 播放诊断支持运行时开关，播放器配置收敛到独立「Audio Player」菜单（判读方法见[播放卡顿排查](docs/troubleshooting/audio-playback-stutter.md)）。
- 重构：音频驱动栈统一迁移 tal_audio——录音、通话、产测、抓流全部从 tkl_audio 迁移，并新增上行 5 路音频流 SD 卡抓取分析能力（见[音频上行 5 路抓流](docs/troubleshooting/audio-stream-capture.md)）。
- 新增：外部存储统一为 `wukong_storage` 层——介质（SD 卡/外挂 QSPI Flash/无）编译期单选、全 app 唯一一次开机异步挂载，原 store 记录层并入，路径与字节格式完全兼容、OTA 免迁移（接入见 `src/wukong/storage/README.md`）。
- 新增：P2P 通话整体升级——更换 P2P 业务组件（TMM 三层栈），新增设备↔设备语音通话、联系人页与 NFC 碰一碰加好友，来电改为默认响铃手动接听（安全合规）。
- 新增：首启激活向导——未激活设备开机自动进入语言选择→激活方式→扫码激活流程，支持 WLAN 页手动连网（屏上软键盘）与屏上二维码直连扫码配网激活。
- 新增：现场诊断能力——应用中心新增诊断页，收纳系统状态（内存/运行时长/复位原因）、网络诊断（WiFi/IP/RSSI/云连接）、屏幕测试（纯色/彩条/触摸画线）、音频诊断、播放统计与帧率显示，排查现场问题不用接串口。
- 修复：语音播报中插话打断后，新回复文本拼接进上一条 AI 消息的问题——打断后一律另起新消息，覆盖云端与设备端两类打断路径。
- 优化：时间管理体验——时钟页统一闹钟/倒计时/秒表/番茄钟四 tab 视觉层级，下拉面板新增计时卡片网格实时显示运行状态并可直达对应标签，状态栏闹钟/日程实时更新。
- 优化：相机链路重构——取景器重构、预览/大图降采样、拍照结果回调，侦测模式增加相机能力校验。
- 优化：录音功能完善——SD 写盘解耦防丢帧、播放进度秒级刷新与 hh:mm:ss 时长、列表倒序、转写结果翻页展示、掉电恢复改惰性 MD5 加速上电。
- 优化：开机体验——开机画面提前约 1 秒（LCD→splash→TP 初始化重排、TP 非阻塞）、开机动画迁移 TEF 并瘦身。
- 优化：固件资源瘦身——BSS 改造回收约 34.7KB SRAM（静态缓冲按需分配/移出 BSS），中文字库裁剪至一级常用字 + Montserrat 字形子集共省约 435KB flash，UI 线程栈收敛到 menuconfig。
- 新增：超低功耗 ULTRA_SLEEP 模式（AP 长保活）。
- 新增：显示总开关 `ENABLE_TUYA_DISPLAY`——LCD/TP 驱动栈与显示消息总线独立于 UI 框架编译，只开显示不开 UI 时业务仍上报表情/对话状态供板级自有渲染消费。
- 新增：系统升级（OTA）页面——设置 → 关于新增「系统升级」入口，支持主动检查更新（展示目标版本、包大小与更新说明）并确认升级；OTA 开始自动进入升级页，进度条 + 阶段文案实时展示，下载阶段可返回并从下拉面板续看进度，校验/安装期锁定页面、完成后自动重启。
- 优化：OTA 默认启用压缩升级。
- 文档：docs/ 重构为任务导向体系——howto 系列（适配新板/AI 技能/唤醒词/对话模式/MCP 工具/定制 UI/TEF 动图/[成品产测](docs/howto/factory-test.md)/[对接 UART 离线语音芯片](docs/howto/connect-uart-voice-chip.md)等）+ 排障指南系列 + 模块参考索引 + 资源统计，全部中英双语并配硬检查脚本。
